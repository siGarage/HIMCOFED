'use strict';

/**
 * photo-gallery-car controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::photo-gallery-car.photo-gallery-car');
